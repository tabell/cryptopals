#include <stdio.h>

int main(int argc, char** arv) {
    printf("chal 8 is not done\n");
    printf("reading file of possible ciphertexts\n");
    printf("for each ciphertext: produce the best plaintext guess, score each guess, choose the higehst score");
    return 0;
}
